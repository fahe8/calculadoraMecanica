import 'package:flutter/material.dart';

class CrearBomba extends StatelessWidget {
const CrearBomba({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
} 